# 0521

- 实现硬编码匹配酒馆原生元素

# 0524

- 构建扫描机制匹配第三方插件按钮
- 通过 `.inline-drawer-header` 特征检索完成 `#extensions_settings` 匹配
- 引入列表去重机制

# 0525

- 修复 `#extensionsMenu` 匹配问题：
  - `itemMatch` 新增 `display: none` 跳过 — 排除隐藏备用按钮
  - 新增 `alsoMatchChildren` fallback 匹配 `.list-group-item` 以外的直接子元素
  - `init()` 添加 3s 延迟重扫描解决注入时序漏匹配
- 新增预设面板分组（`#left-nav-panel`），按功能分类隐藏
- 新增重排序功能：
  - 页签切换"隐藏元素"/"重排序"
  - HTML5 Drag & Drop 拖拽排序，实时应用到页面 DOM
  - 动态查找最深公共祖先作为重排序容器（`applyReorder`），兼容不同 DOM 结构
  - 隐藏元素自动过滤，取消隐藏后追加到末尾
  - 首次打开从 PANEL_GROUPS + 当前 DOM 初始化排序
- 修复拖动后分组收起：`renderReorderView` 保存/恢复展开状态

# 0526

- 修复 IDE/linter 报错：`style.css` 9处 CSS 自定义属性未声明、`index.js`/`content.js` inline `var(--muted)`
- 魔棒菜单中"酒馆菜单精简器"入口加入可隐藏列表（移除 exclude）
- 注册 `/menucleaner` 斜杠命令作为备用面板入口：
  - 扩展版本通过 `import { registerSlashCommand }` 注册
  - 酒馆助手版本通过向父文档注入 `<script type="module">` 注册
- 修复酒馆助手版本"打开操作面板"按钮竖排：CSS 补全 `#menu-cleaner-settings` 系列选择器
- `style.css` 新增 `--SmartThemeBlurTintColor` 等变量默认值声明（在 `.menu-cleaner-popup` 作用域）
